<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password?</title>

    <!-- custom css -->
    <link rel="stylesheet" href="../css/forgot.css">

    <!-- google icons -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />

    <!-- boxicons css -->
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
</head>
</head>
<body>
    <div class="container">
        <h2 class="heading">Find Your Account</h2>
        <h5 style="text-align: left;" class="message-heading">Please enter your email address or mobile number to search for your account.</h5>
        {{#if error}}<h6 class="error-message">{{error}}</h6>{{else}}{{/if}}
        {{#if message}}<h6 class="success-message">{{message}}</h6>{{else}}{{/if}}
        <div class="content-box">
            <form action="" class="sign-up" method="POST">
                <div class="input-box">
                    <input name="email" type="text" placeholder="Email address" required autocomplete="on">
                </div>
                <button type="submit">Search</button>
                <button type="submit" >Cancel</button>
            </form>
        </div>
    </div>
</body>
</html>